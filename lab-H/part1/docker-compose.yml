version: "2.2"

services:
  server:
    build: server/.
  
  client:
    build: client/.
    privileged: true # Need to add privileged mode so mount works!!!
    links:
      - server
    depends_on:
      - server